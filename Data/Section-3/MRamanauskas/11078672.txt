#include <iostream>
#include <cmath>  
#include <math.h>  
#include <cstdlib>
#include <climits>

using namespace std;


int main()
{
    long long n,k,max,l,a1,a2,a3,a4,b1,b2,b3,b4;
	long long m[4][4];
	int y;
	char r[100];
	cin>>a1;
	y=sqrt(2*a1*1.);
	if(y*(y+1)/2==a1)
		cout<<"YES";
	else
		cout<<"NO";
	
		

	//system("pause");
	return 0;
}
