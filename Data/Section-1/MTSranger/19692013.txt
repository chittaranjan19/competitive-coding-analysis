#include <iostream>

using namespace std;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int n; cin >> n;
	for (int i = 0; i < n; i++) {
		cout << "I " << (i%2==0 ? "hate " : "love ");
		if (i+1 < n) cout << "that ";
	}
	cout << "it" << endl;
}
