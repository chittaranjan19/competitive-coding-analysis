#include <bits/stdc++.h>
#define FORE(i, a, b) for(int i = a; i <= b; i++)
#define FORD(i, a, b) for(int i = a; i >= b; i--)
#define FOR(i, a, b) for(int i = a; i < b; i++)
const int MAXN = 1e5 * 5;
const int INF = 1e9 + 7;

using namespace std;
string s;
int a, b, n;
int main()
{
    ios::sync_with_stdio(0); cin.tie(0);
    #ifndef ONLINE_JUDGE
    //freopen("612A.inp", "r", stdin);
    //freopen("612A.out", "w", stdout);
    #endif //MIKELHPDATKE
    cin >> n >> a >> b;
    cin >> s;
    FORE(x, 0, n / a) FORE(y, 0, n / b) if ()
    return 0;
}

