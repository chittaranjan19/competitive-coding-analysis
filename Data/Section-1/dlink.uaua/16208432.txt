#include <bits/stdc++.h>

#define out_files freopen("new_rules.in", "r", stdin); freopen("new_rules.out", "w", stdout)

using namespace std;

int main()
{   //out_files;

    int n;
    cin>>n;
    if(n%2==1) cout<<1;    else cout<<2;
    return 0;
}
