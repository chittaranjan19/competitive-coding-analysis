#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>

#define wl while
using namespace std;
char a[100];
int main()
{
    int n;
    wl(scanf("%d",&n)!=EOF)
    {
       int i;
       getchar();
       gets(a );
       gets(a );
              gets(a );
       printf("2\n");
    }
    return 0;
}
