#include <bits/stdc++.h>

#define SZ(x) ((  int( x.size() ) ))

using namespace std;

const int maxn = 1000 + 10;

int n,m;

int main(){
	cin >> n >> m;
	if(n > m)
		cout << "First" << endl;
	else{
		cout << "Second" << endl;
	}
	return 0;
}
