#include<bits/stdc++.h>
using namespace std;
int main(){
        long long a,b,c;
cin>>a>>b>c;
cout<<(a/c+(a%c!=0?1:0))*(b/c+(b%c!=0?1:0))<<endl;
	/*memset(dp,-1,sizeof(dp));
	F[0]=P[0]=1;
	for(int i=1;i<MAXN;i++){
		F[i]=mod(F[i-1]*i);
		P[i]=mod(Fe(F[i],INF-2));
	}
	scanf("%d%d",&n,&k);
	for(int i=1;i<=n;i++){
		int x;scanf("%d",&x);
		if(lucky(x))
			pm[x]++;
		else
			unlucky++;	
	}
	tr(it,pm)
		luc.pb(it->ss);
	printf("%lld\n",rec(0,0));*/	
	return 0;
}
//LooK aT mY COde ONlinE +_+
