#include <bits/stdc++.h>
#define FOR(i, j, k) for (int i = j; i <= k; i ++)
#define FORD(i, j, k) for (int i = j; i >= k; i --)
#define mn 100100
#define base1 7
#define maxC 1000000007
#define F first
#define S second
#define mod(x) (x & (-x))

using namespace std;
long long n;

void setup ()
{
    cin >> n;
    cout << (n % 3 == 0? (n / 3) * 2 : (n / 3) * 2 + 1);
}

void xuly()
{

}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    setup ();
    xuly ();
    return 0;
}
