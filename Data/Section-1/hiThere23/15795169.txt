#include <bits/stdc++.h>
#define FOR(i, j, k) for(int i = j; i <= k; i ++)
#define FORD(i, j, k) for(int i = j; i >= k; i --)
#define mn 100001
#define off 3000
#define maxC 1000000007
#define F first
#define S second
#define mp make_pair

using namespace std;
double d, l, v1, v2;

void setup()
{
    cin >> d >> l >> v1 >> v2;
    cout << fixed << setprecision(7) << (l - d) / (v1 + v2);
}

void xuly()
{

}

int main()
{
    iostream::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    setup();
    xuly();
    return 0;
}
