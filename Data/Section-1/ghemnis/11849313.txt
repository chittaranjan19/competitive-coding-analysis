#include <bits/stdc++.h>

#define fr for(i=0;i<n;i++)
#define st first
#define nd second
#define pb push_back
#define PB pop_back()
#define sqr(a) (((a) * (a)))
#define mp make_pair

using namespace std;

typedef long long int LL;
typedef pair<int,int> pii;
typedef vector<int> vi;

int n,m,i,j,k,l,T,t,c;
int a[200010];

int main ()
{
    cin>>n>>t>>c;
    fr
    {
        scanf("%d",&a[i]);
    }
    sort(a,a+n);
    for(i=0;a[i]<=t and i<n;i++)
    {

    }

    cout<<i-c+1;
}
